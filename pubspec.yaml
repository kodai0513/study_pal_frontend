name: study_pal_frontend
publish_to: none
description: Minimal Flutter TODO app with MVVM
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  hooks_riverpod: ^2.6.1
  built_collection: ^5.1.1
  dio: ^5.8.0+1
  openapi_generator_annotations: ^6.1.0
  openapi:
    path: ./openapi

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
